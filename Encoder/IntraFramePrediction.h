#include <common.h>

struct MDIFF IntraFramePrediction(unsigned int row, unsigned int col, unsigned int width, unsigned int height, unsigned int block, unsigned char* CUR_FRAME);

struct MDIFF IntraFramePrediction(unsigned int row, unsigned int col, unsigned int width, unsigned int height, unsigned int block, unsigned char* CUR_FRAME) {
	// TODO
	struct MDIFF temp;
	temp.MODE = 0;
	temp.RUN = 0;

	return temp;
}