#include <IntraFramePrediction.h>

struct GMV IntraFramePrediction(unsigned int row, unsigned int col, unsigned int width, unsigned int height,unsigned int block, unsigned char* CUR_FRAME) {
	// TODO
	struct GMV temp;
	temp.X = 0;
	temp.Y = 0;

	return temp;
}